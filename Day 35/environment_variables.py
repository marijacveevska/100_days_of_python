"""
Environment variables 
Environment variables are named values stored by the operating system or runtime environment 
that programs can read while running. 
They’re commonly used to store configuration or secrets without putting them directly inside your code.
"""